# 작업일지 (FE-S14P31A204-352/351 연관)

작성일: 2026-05-12
작성자: Codex 협업 로그 정리

## 1. 목표
- REST polling 제거 후 WebSocket 기반(`s3Url`) 동기화로 전환
- 초대 모달 UX/초대자 관리 흐름 정리
- 핀/댓글 작성자명 표시 보정
- 에디터 층(Layer) 편집 정책 정리
  - 층 선택은 가능
  - 하위 층은 읽기 전용
  - 자동저장/스냅샷 일관성 보장

## 2. 주요 변경 이력

### 2.1 Polling 구조 제거
- `floorPlanGenerate.service.ts`에서 polling 관련 미사용 블록 제거 방향으로 정리
- WebSocket payload의 `s3Url` 사용 중심으로 전환
- 쟁점:
  - `s3Url` 누락이 아니라 `floorPlanPayloadJson.layout` 비어있는 케이스가 복원 실패 원인일 수 있음

### 2.2 초대 모달/초대자 관리 개선
- 이미 초대된 사용자 태그 노출
- Enter 기반 검색 UX로 정리(정확 이메일 입력 흐름)
- 초대 삭제 API 연결 방향 검토
  - `DELETE /api/v1/projects/{projectId}/members/{userId}`
- 대표 고객 지정 기능 제거 합의 반영

### 2.3 댓글/핀 작성자명 이슈 대응
- fallback 처리/이름 매핑 로직 검토
- 실명 표시 실패 구간 분리 점검

### 2.4 BE 연동 관찰 결과 정리
- IFC 생성 워커 완료 + 소켓 브로드캐스트는 확인
- 재입장 시 복원 실패 원인 후보:
  - generate 경로에서 history/DB 영속 갱신 누락 가능성
  - 상세 API 응답에서 특정 조건(`bubbleEditing`)에 따른 `ifcStorageUrl` null 처리 가능성

### 2.5 층 추가/층 편집 정책 구현 진행
- 1층 기본 생성/층 추가 시 2층 생성 흐름 구현 시도
- overlay 표현/편집 제한(하위층 read-only) 구현 반복
- 적용된 핵심 가드:
  - 룸 이동/리사이즈/폴리곤 편집 차단
  - 벽/개구부 생성/수정/삭제 차단
  - 속성 패널(label/type/size) 편집 차단
- 층 추가 시 자동저장 트리거 추가
  - `markLocalFloorPlanSnapshotChanged()` 호출

## 3. 현재 상태(요약)
- 빌드는 통과(`npm run build`)
- 하위층 read-only 가드는 핵심 편집 경로에 반영됨
- 남은 UX 정합성 이슈:
  - 층 버튼 활성/비활성 시각 상태가 기대와 다름
  - 층 전환 시 1층 기본 bubble이 같이 움직여 보이는 현상(스냅샷/전환 로직 추가 점검 필요)

## 4. 다음 작업 제안
1. `TwoDLeftPanels.tsx`에서 "선택된 층만 활성 스타일"로 고정
2. 층 전환 시 스냅샷 저장/복원 로그를 붙여 실제 데이터 번짐 경로 확인
3. bubble 모드 복귀 시 기준층(1층) 복원 정책 확정
4. QA 시나리오 고정
   - 1층 편집 -> 2층 생성 -> 2층 편집 -> 1층 재선택 -> 데이터 불변 검증

## 5. 변경 파일(핵심)
- `FE/src/features/editor/hooks/useEditorPage.ts`
- `FE/src/features/editor/hooks/useEditorStructureEditHandlers.ts`
- `FE/src/features/editor/hooks/useEditorAttributePanelHandlers.ts`
- `FE/src/features/editor/components/layout/TwoDLeftPanels.tsx` (진행 중)
- `FE/src/features/editor/components/panels/InspectorPanel.tsx` (진행 중)
- `BUBBLE_JSON_FLOW.md` (문서 보강)

